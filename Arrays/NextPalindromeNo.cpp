#include<iostream>

void NextPalindromeNo( int arr[], int size){
    int mid= size/2;
    if( element == mirror){
        return;
    }
    else if (element != mirror){
        mirror = element;
    }
}
int main(){

    int arr[] = { 2, 4, 5, 6, 6, 5, 3, 1};
    int size = sizeof(arr)/sizeof(arr[0])-1;


}
#include<iostream>
#include "LinkedList.h"

int getNode( struct Node* start, int position){
    if( start->next->number == position){
        
    }
    
    
}

int main(){
    struct Node * start=NULL;
    InsertInBeginning( &start, 1);
    InsertInBeginning( &start, 2);
    InsertInBeginning( &start, 3);
    InsertInBeginning( &start, 4);
    parseList(start);
}

